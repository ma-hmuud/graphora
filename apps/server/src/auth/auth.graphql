type User {
  id: ID!
  name: String!
  email: String!
  emailVerified: Boolean!
  image: String
  passwordHash: String
  plan: Plan!
  createdAt: String!
  updatedAt: String!
}

enum Plan {
  FREE
  PRO
}
